services:
  - type: web
    name: pdf-to-pptx-free
    env: python
    plan: free # Strictly tells Render to use the free tier
    buildCommand: |
      apt-get update && apt-get install -y tesseract-ocr
      pip install -r requirements.txt
    startCommand: gunicorn app:app
